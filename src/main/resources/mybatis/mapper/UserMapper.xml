<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="first.example.firstclass.dao.UserDAO">
	<!-- 회원가입  -->
	<insert id="save" parameterType="JoinDTO">
        INSERT INTO TB_User (
            name,
            registration_number,
            zip_code,
            address_base,
            address_detail,
            username,
            password,
            role,
            DELT_AT
        ) VALUES (
            #{name},
            #{registrationNumber},
            #{zipCode},
            #{addressBase},
            #{addressDetail},
            #{username},
            #{password},
            #{role},
            #{deltAt}
        )
    </insert>
    
    <!-- 로그인 확인  -->
    <select id="findByUsername" parameterType="string" resultType="UserVO">
    	SELECT username, password, role
    	From TB_User
    	WHERE username = #{username}
    </select>
</mapper>